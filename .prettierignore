out
node_modules
package-lock.json
apps/api/src/worker-configuration.d.ts

# エージェント用スキルの取得物。自分たちが書いたコードではないので整形対象にしない
# （prettier が解析に失敗するファイルも含まれ、format:check 全体が落ちる）
.agents/skills
.claude/skills
skills-lock.json

# TanStack Router が生成するルートツリー。ビルドのたびに作り直されるので
# 手で直さない（git には追跡させる。実行時に使われる正真正銘のソースである）。
apps/web/src/client/routeTree.gen.ts
