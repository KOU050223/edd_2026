API_ORIGIN=http://localhost:8787
# Auth0 の Web アプリ（regular_web）の client secret。docs/auth.md §5 を参照。
# client_id と issuer は秘密ではないので wrangler.jsonc の vars にある。
AUTH_CLIENT_SECRET=replace-with-the-auth0-web-client-secret
